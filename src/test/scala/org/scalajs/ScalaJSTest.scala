package org.scalajs

import org.specs.SpecsMatchers
import org.junit.Test
import org.mozilla.javascript.{ScriptableObject, Context, ContextFactory}

class ScalaJSTest extends SpecsMatchers {

  @Test
  def basicTest() {
    /*
    val pkg: ScalaJSPackage = new ScalaJSPackage(
      classes = List(),
      objects = List(),
      functions = List()
    )
    val script = """
    function x() {5}
    """

    */
  }
}